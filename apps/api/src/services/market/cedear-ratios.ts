/**
 * SHIM de compatibilidad — la implementación real vive en @sentinel/domain.
 * Los tests viejos siguen importando de este path hasta su migración (commit 7).
 */
export * from "@sentinel/domain";
