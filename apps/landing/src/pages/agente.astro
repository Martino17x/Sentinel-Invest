---
import Base from "../layouts/Base.astro";
import AgentMCP from "../components/AgentMCP.astro";
import CTA from "../components/CTA.astro";
---
<Base
  title="Agente IA — Sentinel Invest"
  description="Conectá tu agente de IA vía MCP y leé tu cartera y cotizaciones en tiempo real. Sentinel App: consulta y opera (POST /api/orders tras opt-in IOL_TRADING_ENABLED + confirmación explícita y auditoría). Sentinel MCP: solo lectura (scope read) + trading opcional (scope trade)."
>
  <AgentMCP />
  <CTA />
</Base>
